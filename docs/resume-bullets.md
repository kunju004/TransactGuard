# Resume Bullets

Use these once the repo is public and runnable.

```latex
\textbf{TransactGuard} \hfill Jul. 2026 -- Present
\begin{itemize}[leftmargin=*, itemsep=0.3pt, topsep=1pt]
\item Built a \textbf{Java 21/Spring Boot} payment-risk authorization microservice with \textbf{REST APIs}, Bean Validation, idempotency keys, account locking, and \textbf{JPA/Hibernate} persistence using PostgreSQL and H2 profiles.
\item Implemented transactional outbox messaging with a \textbf{Kafka-ready} event publisher, after-commit relay, correlation IDs, Docker Compose deployment, and CI validation through \textbf{GitHub Actions}.
\item Added \textbf{JUnit/MockMvc} test coverage, Swagger/OpenAPI docs, actuator health checks, secure headers, and \textbf{Prometheus} metrics for validation, persistence, idempotency, and API behavior.
\end{itemize}
```
